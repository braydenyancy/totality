---
description: Health check for the totality plugin pack. Verifies required plugins, MCP tooling, and freshness. Halts on hard failures with copy-pasteable install commands.
---

Use the `doctor` skill in **standalone** mode — print a full report covering required plugins, recommended plugins, external tooling, and self-version. Surface every check result, pass or fail.
