---
description: Walk through a debrief of a totality miss (or surprising win) and write a case study to knowledge/feedback/<slug>.md. Future rnd runs ingest these as prior lessons.
argument-hint: "[short label for the incident — e.g. audio-visual-mapping miss]"
---

Use the `debrief` skill to capture a structured case study from the user.

User input: $ARGUMENTS

Run the skill's loop exactly: ground in prior knowledge, then ask one question at a time, verify each answer against the codebase before continuing, and only write the artifact after the user confirms the synthesis. Do not batch questions where a later question depends on an earlier answer.
